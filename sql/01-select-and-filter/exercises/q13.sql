-- q13 (core) — Which regions run which kinds of cost centre?
--
-- The distinct combinations of region and cost centre type.
--
-- Expected output: Two columns — region, cost_centre_type — one row per
-- combination that exists, region order then type.
--
-- Hint: DISTINCT applies to the whole row you selected, not to one column.

-- Write your query below. One statement, ending in a semicolon.

