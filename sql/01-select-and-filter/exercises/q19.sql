-- q19 (stretch) — Currency and country pairs
--
-- The distinct combinations of country and currency on the customer
-- master, so you can see which countries we invoice in which currency.
--
-- Expected output: Two columns — country, currency — one row per
-- combination, country order then currency. Customers with no country still
-- form a group.
--
-- Hint: Think about where the NULL country sorts, and whether that is what
-- you want on a report.

-- Write your query below. One statement, ending in a semicolon.

