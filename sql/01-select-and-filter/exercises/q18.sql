-- q18 (stretch) — A one-row control summary of the invoice table
--
-- Produce a single row that shows: how many invoice rows there are, how
-- many have a customer, and how many have a supplier. Use it to prove
-- that every invoice is either a sale or a purchase and never both.
--
-- Expected output: Three columns — invoice_rows, with_customer,
-- with_supplier — one row.
--
-- Hint: count(*) counts rows; count(column) counts non-NULL values. Do the
-- two smaller numbers add up to the big one?

-- Write your query below. One statement, ending in a semicolon.

