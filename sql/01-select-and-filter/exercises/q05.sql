-- q05 (warmup) — What payment methods are in use?
--
-- List the distinct payment methods in the payments table.
--
-- Expected output: One column, one row per method, alphabetical.
--
-- Hint: More values than you expect — the casing is inconsistent. That is
-- the finding, not a mistake in your query.

-- Write your query below. One statement, ending in a semicolon.

