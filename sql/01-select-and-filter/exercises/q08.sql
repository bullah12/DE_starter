-- q08 (warmup) — Exchange rates per thousand units
--
-- The first ten exchange rate rows by date then currency, showing the
-- date, currency, the rate, and what 1,000 units of that currency is
-- worth in GBP.
--
-- Expected output: Four columns — rate_date, from_currency, rate,
-- gbp_per_1000 — ten rows.
--
-- Hint: Multiply the rate by 1000 and round to two decimal places.

-- Write your query below. One statement, ending in a semicolon.

