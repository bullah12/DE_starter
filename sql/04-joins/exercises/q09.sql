-- q09 (core) — Revenue by region and year
--
-- Sales invoices, by the region of the owning cost centre and the
-- calendar year of the due date. The board wants to see the trend.
--
-- Expected output: Four columns — region, year, invoice_count, gross_gbp —
-- one row per region and year, ordered by region then year.
--
-- Hint: Three tables. year(due_date) gives you the year. The invoice whose
-- customer does not exist will be dropped by an INNER JOIN — for this
-- question that is acceptable.

-- Write your query below. One statement, ending in a semicolon.

