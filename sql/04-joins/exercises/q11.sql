-- q11 (core) — Purchase spend by supplier country
--
-- Total purchase invoice value by supplier country, showing only
-- countries where we have spent more than £250,000 in total.
--
-- Expected output: Three columns — country, supplier_count, total_gbp — one
-- row per qualifying country, largest first.
--
-- Hint: HAVING filters groups, WHERE filters rows. Count distinct
-- suppliers, not invoices.

-- Write your query below. One statement, ending in a semicolon.

