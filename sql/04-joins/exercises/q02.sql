-- q02 (warmup) — Ten largest sales invoices
--
-- List the ten largest sales invoices by GBP value, showing the customer
-- name alongside.
--
-- Expected output: Four columns — invoice_id, customer_name, due_date,
-- gross_amount_gbp — ten rows, largest first.
--
-- Hint: invoices is your population. One customer per invoice, so an INNER
-- JOIN adds no rows.

-- Write your query below. One statement, ending in a semicolon.

