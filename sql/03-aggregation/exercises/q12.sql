-- q12 (core) — Journals per source per year
--
-- How many journals were raised from each source in each calendar year?
--
-- Expected output: Three columns — year, source, journal_count — year then
-- source.
--
-- Hint: year(journal_date) as a grouping column. You can group by an
-- expression, not just a bare column.

-- Write your query below. One statement, ending in a semicolon.

