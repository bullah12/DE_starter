-- q01 (warmup) — Size up the invoice table
--
-- One row showing the number of invoices, the total gross in GBP, and the
-- smallest and largest gross GBP values.
--
-- Expected output: Four columns — invoice_count, total_gbp, smallest_gbp,
-- largest_gbp — one row.
--
-- Hint: No GROUP BY: the whole table is one group.

-- Write your query below. One statement, ending in a semicolon.

