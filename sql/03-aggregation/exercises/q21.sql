-- q21 (stretch) — Average invoice by month and type
--
-- The average invoice value in GBP by calendar month and invoice type,
-- but only for month and type combinations with at least twenty
-- invoices.
--
-- Expected output: Five columns — month, invoice_type, invoice_count,
-- average_gbp, total_gbp — month then type.
--
-- Hint: Two grouping columns and a HAVING on the count. Use due_date for
-- the month, since invoice_date is text in three formats.

-- Write your query below. One statement, ending in a semicolon.

