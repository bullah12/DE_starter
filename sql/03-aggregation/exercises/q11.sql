-- q11 (core) — Customer invoice profile
--
-- For every customer that has been invoiced, the number of sales
-- invoices, the total, the average and the largest, all in GBP.
--
-- Expected output: Five columns — customer_id, invoice_count, total_gbp,
-- average_gbp, largest_gbp — largest total first.
--
-- Hint: Four aggregates over the same group. Round the average.

-- Write your query below. One statement, ending in a semicolon.

