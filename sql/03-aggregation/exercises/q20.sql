-- q20 (stretch) — Revenue concentration
--
-- How much of our sales value comes from the top customers? For each
-- customer show their total sales and what percentage of all sales that
-- represents, for the ten largest.
--
-- Expected output: Three columns — customer_id, total_gbp, pct_of_total —
-- largest first.
--
-- Hint: The denominator is a total over the whole table while you are
-- grouping by customer. A scalar subquery does it; a window function (topic
-- 06) does it more elegantly. Either is a fair answer here.

-- Write your query below. One statement, ending in a semicolon.

