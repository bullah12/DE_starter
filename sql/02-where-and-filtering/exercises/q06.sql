-- q06 (warmup) — Suppliers whose name mentions metals or tooling
--
-- Find every supplier whose name contains 'metals' or 'tooling', regardless
-- of how it is capitalised.
--
-- Expected output: Three columns — supplier_id, supplier_name, country —
-- supplier_id order.
--
-- Hint: ILIKE with % either side. Plain LIKE would miss most of them.

-- Write your query below. One statement, ending in a semicolon.

