-- q07 (warmup) — Mid-sized credit limits
--
-- Customers whose credit limit is between £50,000 and £150,000 inclusive.
--
-- Expected output: Three columns — customer_id, customer_name,
-- credit_limit_gbp — largest limit first, then customer_id.
--
-- Hint: BETWEEN includes both ends, which is what 'inclusive' means.

-- Write your query below. One statement, ending in a semicolon.

