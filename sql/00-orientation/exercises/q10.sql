-- q10 (core) — How many customers have a country?
--
-- Count the customer rows, and separately count how many of them have a
-- country recorded. Call the columns customer_rows and with_country.
--
-- Expected output: Two columns, one row.
--
-- Hint: count(*) counts rows; count(column) counts rows where that column
-- is not NULL. The difference between the two numbers is the number of
-- missing values.

-- Write your query below. One statement, ending in a semicolon.

