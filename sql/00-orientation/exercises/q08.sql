-- q08 (warmup) — Is payment_id a key?
--
-- Run the same check on payment_id in the payments table.
--
-- Expected output: Two columns — payment_id and times_it_appears — one row
-- per duplicated id, id order.
--
-- Hint: Same recipe. This one does return rows, and they matter: a
-- duplicated payment is cash counted twice.

-- Write your query below. One statement, ending in a semicolon.

