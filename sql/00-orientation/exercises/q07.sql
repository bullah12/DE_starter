-- q07 (warmup) — Is invoice_id a key?
--
-- Check whether invoice_id uniquely identifies a row in invoices. Show
-- any value that appears more than once, and how often.
--
-- Expected output: Two columns — invoice_id and times_it_appears. If the
-- column is a genuine key, you will get no rows at all.
--
-- Hint: Copy the recipe from examples/04_keys.sql and change the table and
-- column. No rows back is the good outcome.

-- Write your query below. One statement, ending in a semicolon.

