-- q12 (core) — Is a ledger line identified by journal and line number?
--
-- Check whether the combination of journal_id and line_number uniquely
-- identifies a general ledger row.
--
-- Expected output: Three columns — journal_id, line_number,
-- times_it_appears. No rows means the combination is unique.
--
-- Hint: A key can be made of more than one column. Group by both.

-- Write your query below. One statement, ending in a semicolon.

