-- The asterisk means "every column". Useful when you are exploring a table
-- for the first time and do not yet know what is in it.
--
-- Do not leave * in a report. Someone adds a column upstream and your output
-- silently changes shape.

SELECT *
FROM cost_centres;
