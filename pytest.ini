[pytest]
# Keep the output short and readable — you are here to learn SQL and Python,
# not to read stack traces from the test runner itself.
addopts = -q --tb=short -rs --import-mode=importlib
testpaths = sql python projects
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
